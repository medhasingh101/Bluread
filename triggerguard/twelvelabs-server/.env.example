# Twelve Labs — get key and create index at https://playground.twelvelabs.io
TWELVE_LABS_API_KEY=
TWELVE_LABS_INDEX_ID=
# Gemini — for /analyze-image (single frame). Use a key with quota so extension can avoid 429.
GEMINI_API_KEY=
PORT=4001
